import { ProgressCircleDirective } from './progress-circle.directive';

describe('ProgressCircleDirective', () => {
  it('should create an instance', () => {
    // const directive = new ProgressCircleDirective();
    // expect(directive).toBeTruthy();
  });
});
